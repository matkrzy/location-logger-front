export const styles = theme => ({
  detailsPanel: {
    display: 'flex',
    flexWrap: 'wrap',
    paddingTop: theme.spacing.unit * 2,
  },
  chart: {
    margin: `${theme.spacing.unit * 3}px 0 0`,
  },
  chartSelector: {
    display: 'flex',
    flexDirection: 'row'
  },
});
